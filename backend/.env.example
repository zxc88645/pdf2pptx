# 上傳與輸出目錄（可選，預設為 backend 下的 uploads / outputs）
# UPLOAD_DIR=uploads
# OUTPUT_DIR=outputs

# PDF 轉圖 DPI（預設 200，愈高愈清晰、檔案愈大）
# PDF_DPI=200

# 啟動時預載 LaMa 模型（預設 1），可縮短首次 /api/inpaint 延遲；設 0 則延遲到首次請求再載入
# PRELOAD_LAMA=1
