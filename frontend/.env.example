# 後端 API 基底網址（部署到 GitHub Pages 時需設定）
#
# 建議不要結尾 `/`，前端會自動加上 `/api/inpaint` 等路徑，例如：
# VITE_API_BASE=https://your-backend.example.com
#
# 本機開發若用 Vite proxy，可留空：
VITE_API_BASE=https://pdf2pptx.itr-lab.cloud
